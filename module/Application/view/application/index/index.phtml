<!-- Abaixo do formulário de adicionar -->
<div class="row">
    <?php foreach ($contents as $content) : ?>
        <div class="col-md-12">
            <p><?php echo $content['id']." ".$content['content']." ".$content['subcontent']; //echo $content->getContent(); ?></p>
        </div>
    <?php endforeach; ?>
</div>